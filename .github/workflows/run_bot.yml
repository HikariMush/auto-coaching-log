name: SZ Auto Logger Ultimate

on:
  workflow_dispatch: # 手動実行ボタン（テスト用）
  schedule:
    - cron: '0 0,12 * * *' # 毎日 UTC 0:00(JST 9:00) と 12:00(JST 21:00)

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 6時間（念の為長めに）

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          python -m pip install --upgrade pip
          pip install google-genai groq patool google-api-python-client google-auth-httplib2 google-auth-oauthlib requests

      - name: Run Auto Logger
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
        run: python main.py
