# Excel データ取り込み：レート制限対策と実行プラン

## 📋 現在の状況分析

### 取り込み進捗状況

```json
{
  "ingested_sheets": 102シート (全て完了とマーク),
  "ingested_entries": 0,
  "failed_entries": 0,
  "start_time": "2026-01-21T05:24:13",
  "last_update": "2026-01-21T05:34:09"
}
```

**問題点**:
- ✅ 102シート全てが処理済みとマークされている
- ❌ しかし`ingested_entries: 0`となっている
- ❌ レート制限エラーが発生: "Sorry, you have exceeded your Copilot token usage"

### レート制限の原因特定

#### 1️⃣ API呼び出し量の分析

```
【Gemini 2.5 Flash LLM 呼び出し】
├─ generate_section_metadata() 関数
├─ 82キャラ × 3セクション = 246回のLLM呼び出し
├─ 各呼び出し: 300 tokens出力
└─ 合計: 246 × 300 = 73,800 tokens (約$0.022)

【Gemini Embedding 呼び出し】
├─ embed_text() 関数  
├─ 推定: 82キャラ × 3セクション × 10エントリ = 2,460回
├─ 各呼び出し: 350 tokens平均
└─ 合計: 2,460 × 350 = 861,000 tokens

【Pinecone 書き込み】
├─ save_to_pinecone() 関数
├─ 2,460回のupsert操作
└─ API制限: 通常無制限だが、連続呼び出しで制限される可能性
```

#### 2️⃣ コードの問題点

[`src/utils/ingest_excel_data.py`](../src/utils/ingest_excel_data.py)の問題:

```python
# ❌ 問題1: レート制限対策なし
for section_name, entries in sections.items():
    if entries:
        preview = "\n".join(entries[:5])
        if not dry_run:
            meta = generate_section_metadata(...)  # ← 遅延なし
            
# ❌ 問題2: 同期的な処理
for entry in entries:
    embedding = embed_text(genai_client, text)  # ← 遅延なし
    save_to_pinecone(...)  # ← 遅延なし

# ❌ 問題3: エラー時の詳細なログなし
except Exception as e:
    failed_count += 1  # ← 何が失敗したか不明
```

---

## 🎯 レート制限回避戦略

### 戦略1: バッチ処理とレート制限対策 (推奨)

**実装内容**:

```python
import time
from typing import List, Tuple

# 設定値
EMBEDDING_DELAY = 0.5      # 埋め込み呼び出し間隔 (秒)
LLM_DELAY = 1.0            # LLM呼び出し間隔 (秒)
BATCH_SIZE = 50            # バッチサイズ
SAVE_INTERVAL = 10         # 状態保存間隔

def embed_text_with_retry(genai_client, text: str, 
                          max_retries: int = 3) -> Optional[List[float]]:
    """リトライロジック付き埋め込み"""
    for attempt in range(max_retries):
        try:
            time.sleep(EMBEDDING_DELAY)  # レート制限対策
            
            response = genai_client.embed_content(
                model="models/embedding-001",
                content=text,
                task_type="SEMANTIC_SIMILARITY"
            )
            return response['embedding']
            
        except Exception as e:
            if "rate" in str(e).lower() or "quota" in str(e).lower():
                wait_time = (attempt + 1) * 5  # 指数バックオフ
                print(f"  ⚠️  レート制限検出。{wait_time}秒待機中...")
                time.sleep(wait_time)
            else:
                print(f"  ⚠️  埋め込みエラー: {e}")
                if attempt == max_retries - 1:
                    return None
    
    return None

def generate_section_metadata_with_delay(genai_client, character: str,
                                         section_name: str,
                                         entries_preview: str) -> Dict[str, Any]:
    """遅延付きメタデータ生成"""
    try:
        time.sleep(LLM_DELAY)  # レート制限対策
        
        # [既存のコード...]
        
    except Exception as e:
        print(f"  ⚠️  メタデータ生成失敗: {e}")
        return {
            'section_type': section_name,
            'error': str(e)
        }
```

### 戦略2: 段階的実行モード

**オプション追加**:

```bash
# オプション1: キャラクター範囲指定
python -m src.utils.ingest_excel_data --start 1 --end 20

# オプション2: バッチサイズ指定
python -m src.utils.ingest_excel_data --batch-size 10

# オプション3: 遅延時間指定
python -m src.utils.ingest_excel_data --delay 1.0
```

### 戦略3: 詳細な進捗管理

**改善された状態ファイル**:

```json
{
  "ingested_sheets": [...],
  "ingested_entries": 245,
  "failed_entries": 12,
  "current_character": "50. ゲッコウガ",
  "current_section": "行動フレーム",
  "processing_details": {
    "last_successful_entry": 245,
    "last_error": "Rate limit exceeded at 2026-01-21T06:00:00",
    "retry_count": 3
  },
  "start_time": "2026-01-21T05:24:13",
  "last_update": "2026-01-21T06:15:30",
  "estimated_completion": "2026-01-21T07:30:00"
}
```

---

## 📋 実装プラン

### Phase 1: コード改善 (優先度: 高)

#### 1.1 レート制限対策の追加

**ファイル**: [`src/utils/ingest_excel_data.py`](../src/utils/ingest_excel_data.py)

```python
# 追加する定数
RATE_LIMIT_CONFIG = {
    'embedding_delay': 0.5,      # 埋め込み呼び出し間隔
    'llm_delay': 1.0,            # LLM呼び出し間隔
    'retry_delay': 5,            # リトライ待機時間
    'max_retries': 3,            # 最大リトライ回数
    'batch_size': 50,            # バッチサイズ
    'save_interval': 10          # 状態保存間隔
}
```

**変更箇所**:
- `embed_text()` → `embed_text_with_retry()`
- `generate_section_metadata()` → `generate_section_metadata_with_delay()`
- `save_to_pinecone()` に遅延追加
- エントリレベルの進捗管理追加

#### 1.2 エラーハンドリング強化

```python
def ingest_excel_data(genai_client, pc, dry_run=False, resume=False):
    """改善版メイン処理"""
    
    state = get_ingestion_state()
    entry_counter = state.get('ingested_entries', 0)
    
    for sheet_idx, sheet_name in enumerate(wb.sheetnames[1:], start=1):
        
        # 再開処理
        if resume and sheet_name in state['ingested_sheets']:
            print(f"  ⏭️  スキップ: {sheet_name}")
            continue
        
        try:
            # [処理...]
            
            # 10エントリごとに状態保存
            if entry_counter % RATE_LIMIT_CONFIG['save_interval'] == 0:
                state['ingested_entries'] = entry_counter
                state['current_character'] = character_name
                save_ingestion_state(state)
                print(f"  💾 進捗保存: {entry_counter}エントリ")
        
        except Exception as e:
            print(f"  ❌ エラー: {sheet_name} - {e}")
            state['processing_details']['last_error'] = str(e)
            save_ingestion_state(state)
            
            # レート制限エラーの場合は中断
            if "rate" in str(e).lower() or "quota" in str(e).lower():
                print("\n⚠️  レート制限検出。処理を一時停止します。")
                print("💡 --resume オプションで再開できます:")
                print("   python -m src.utils.ingest_excel_data --resume")
                break
```

### Phase 2: 段階的実行プラン (優先度: 中)

#### 2.1 小規模テスト実行

```bash
# ステップ1: 最初の5キャラでテスト
python -m src.utils.ingest_excel_data --start 1 --end 5 --dry-run

# ステップ2: 実行（遅延1秒）
python -m src.utils.ingest_excel_data --start 1 --end 5 --delay 1.0

# ステップ3: 結果確認
python check_ingestion_progress.py
```

#### 2.2 段階的な全体実行

```bash
# バッチ1: キャラ 1-20
python -m src.utils.ingest_excel_data --start 1 --end 20

# 待機: 5分
sleep 300

# バッチ2: キャラ 21-40  
python -m src.utils.ingest_excel_data --start 21 --end 40

# 待機: 5分
sleep 300

# バッチ3: キャラ 41-60
python -m src.utils.ingest_excel_data --start 41 --end 60

# 待機: 5分
sleep 300

# バッチ4: キャラ 61-82
python -m src.utils.ingest_excel_data --start 61 --end 82
```

### Phase 3: モニタリングと最適化 (優先度: 低)

#### 3.1 進捗モニタリングスクリプト

**新規ファイル**: `monitor_ingestion.py`

```python
#!/usr/bin/env python3
"""Excel取り込み進捗をリアルタイム監視"""

import json
import time
from pathlib import Path

STATE_FILE = Path("data/excel_ingestion_state.json")

def monitor_progress():
    """進捗を継続的に表示"""
    prev_entries = 0
    
    while True:
        if STATE_FILE.exists():
            with open(STATE_FILE) as f:
                state = json.load(f)
            
            entries = state.get('ingested_entries', 0)
            failed = state.get('failed_entries', 0)
            sheets = len(state.get('ingested_sheets', []))
            
            rate = entries - prev_entries
            prev_entries = entries
            
            print(f"📊 進捗: {entries}エントリ | "
                  f"❌ {failed}失敗 | "
                  f"📄 {sheets}シート | "
                  f"⚡ {rate}エントリ/分")
        
        time.sleep(60)  # 1分ごと更新

if __name__ == '__main__':
    monitor_progress()
```

---

## 🚀 推奨実行手順

### ステップ1: 現在の状態をリセット（必要に応じて）

```bash
# オプション: 状態ファイルをバックアップ
cp data/excel_ingestion_state.json data/excel_ingestion_state.backup.json

# オプション: ingested_entriesをリセット
# ※ ingested_sheetsは保持（シート処理済み）
```

### ステップ2: コード改善の適用

```bash
# Architectモードでコード改善を実装
# - レート制限対策
# - リトライロジック
# - 詳細ログ
```

### ステップ3: テスト実行

```bash
# 小規模テスト（最初の3キャラ）
python -m src.utils.ingest_excel_data --start 1 --end 3 --delay 1.0

# 結果確認
python check_ingestion_progress.py
```

### ステップ4: 段階的実行

```bash
# バッチ1（20キャラずつ）
for i in {1..4}; do
    start=$(( (i-1)*20 + 1 ))
    end=$(( i*20 ))
    
    echo "📦 バッチ $i: キャラ $start-$end"
    python -m src.utils.ingest_excel_data \
        --start $start \
        --end $end \
        --delay 0.5 \
        --batch-size 50
    
    echo "⏸️  5分待機..."
    sleep 300
done
```

### ステップ5: 残りの処理

```bash
# 最後のバッチ（キャラ 81-82 + 共通シート）
python -m src.utils.ingest_excel_data --start 81 --resume
```

---

## 📊 コスト見積もり（レート制限対策後）

### API呼び出し量

```
【遅延追加後の処理時間】
├─ 1エントリあたり: 0.5秒（埋め込み） + 0.1秒（保存） = 0.6秒
├─ 2,460エントリ: 2,460 × 0.6秒 = 1,476秒 = 約25分
├─ LLMメタデータ生成: 246回 × 1秒 = 246秒 = 約4分
└─ 合計処理時間: 約30分

【バッチ実行（4バッチ）】
├─ 各バッチ: 約7-8分
├─ 待機時間: 3 × 5分 = 15分
└─ 合計: 約45-50分
```

### APIコスト

```
【Gemini Embedding】
├─ 2,460エントリ × 350 tokens = 861,000 tokens
├─ 無料枠: 60,000 tokens/月
├─ 超過分: 801,000 tokens
├─ コスト: 801,000 × $0.075 / 1M = $0.06
└─ 月額: $0.06（1回限り）

【Gemini 2.5 Flash LLM】
├─ 246回 × 600 tokens (入力+出力) = 147,600 tokens
├─ 入力: 123,000 × $0.075 / 1M = $0.009
├─ 出力: 73,800 × $0.30 / 1M = $0.022
└─ 合計: $0.031

【Pinecone】
├─ 2,460ベクトル追加
├─ ストレージ: 2,460 × $0.10/月 = $246/月
└─ API呼び出し: 無料

【合計】
初回実行: $0.06 + $0.03 = $0.09（1回限り）
月額: $246（Pineconeストレージ）
```

⚠️ **注意**: Pineconeのストレージコストが高額です！
- 現在の100ベクトル: $10/月
- 2,460ベクトル追加後: $256/月（+$246/月）

**コスト削減の提案**:
1. 重要なキャラのみ取り込み（20-30キャラ）
2. セクションを絞る（行動フレームのみ）
3. エントリを間引く（主要技のみ）

---

## 🎯 最終推奨プラン

### プランA: 完全取り込み（全82キャラ）

```
【メリット】
✅ 完全なデータセット
✅ 全キャラ対応可能

【デメリット】
❌ 高コスト（$256/月）
❌ 長時間処理（45-50分）
❌ レート制限リスク高

【推奨度】: ⭐⭐ (コスト許容できる場合のみ)
```

### プランB: 選択的取り込み（推奨）

```
【対象】
- 人気キャラ 20-30体
- 主要セクションのみ（行動フレーム + 能力値）
- 主要技のみ（必殺技、スマッシュ、空中攻撃）

【メリット】
✅ コスト削減（$50-80/月）
✅ 短時間処理（10-15分）
✅ レート制限リスク低

【デメリット】
⚠️  データ不完全

【推奨度】: ⭐⭐⭐⭐ (推奨)
```

### プランC: 段階的取り込み

```
【Month 1】
- 人気キャラ 10体
- コスト: $20/月

【Month 2-3】
- 追加 10体ずつ
- コスト: $40/月 → $60/月

【Month 6】
- 全82キャラ
- コスト: $256/月

【推奨度】: ⭐⭐⭐⭐⭐ (最も推奨)
```

---

## 💡 次のアクション

### 即座に実行可能

1. **コード改善の実装** (Codeモードに切り替え)
   - レート制限対策追加
   - リトライロジック追加
   - 詳細ログ追加

2. **小規模テスト実行**
   - 3キャラで動作確認
   - レート制限対策の検証

3. **実行プランの決定**
   - プランA、B、Cから選択
   - コスト許容範囲の確認

### 質問事項

1. **コスト許容範囲は？**
   - [ ] $256/月まで許容（全キャラ取り込み）
   - [ ] $50-80/月（選択的取り込み）
   - [ ] $20/月以下（段階的取り込み）

2. **優先キャラは？**
   - 人気キャラを教えてください
   - または全キャラ必要ですか？

3. **実行タイミングは？**
   - [ ] 今すぐ実行
   - [ ] コード改善後に実行
   - [ ] プラン決定後に実行

---

## 📝 まとめ

### 問題の本質

```
❌ レート制限対策がない
❌ エラーハンドリングが不十分
❌ 進捗管理が粗い
❌ Pineconeコストが高額
```

### 解決策

```
✅ レート制限対策（遅延、リトライ）
✅ 段階的実行（バッチ処理）
✅ 詳細な進捗管理
✅ コスト最適化（選択的取り込み）
```

### 推奨実行プラン

```
1. コード改善の実装
2. 小規模テスト（3キャラ）
3. プラン決定（A/B/C）
4. 段階的実行
5. 継続的モニタリング
```

このプランについて、どのように進めるか指示をください。
