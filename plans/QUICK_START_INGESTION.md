# Excel取り込み：クイックスタートガイド

## 🚀 すぐに実行する方法

### 前提条件

```bash
# 1. API キー設定確認
echo $GEMINI_API_KEY
echo $PINECONE_API_KEY

# 設定されていない場合
export GEMINI_API_KEY="your_gemini_key_here"
export PINECONE_API_KEY="your_pinecone_key_here"

# 2. 依存関係インストール確認
pip install -r requirements.txt
```

---

## 📋 3つの実行プラン

### プランA: テスト実行（推奨: まずこれから）

**時間**: 2-3分  
**コスト**: $0.00  
**対象**: 最初の3キャラのみ

```bash
# ステップ1: ドライラン（API呼び出しなし）
python -m src.utils.ingest_excel_data --dry-run --start 1 --end 3

# ステップ2: 実行（レート制限対策付き）
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 3 \
    --embedding-delay 1.0 \
    --llm-delay 2.0

# ステップ3: 結果確認
python check_ingestion_progress.py
```

---

### プランB: 選択的取り込み（推奨: コスト重視）

**時間**: 15-20分  
**コスト**: $50-80/月（Pinecone）  
**対象**: 人気キャラ20-30体のみ

#### 事前準備: 優先キャラリスト作成

```bash
# 優先キャラを選択（例）
PRIORITY_CHARS="1,3,6,7,8,11,14,18,21,25,29,31,38,46,50,54,60,61,70,71"
```

#### 実行

```bash
# 方法1: 手動でバッチ実行
python -m src.utils.ingest_excel_data --start 1 --end 1    # マリオ
python -m src.utils.ingest_excel_data --start 3 --end 3    # リンク
python -m src.utils.ingest_excel_data --start 6 --end 6    # カービィ
# ... 優先キャラを順次実行

# 方法2: スクリプト作成（後述）
```

---

### プランC: 段階的全体取り込み（推奨: 時間に余裕がある）

**時間**: 45-90分（待機時間込み）  
**コスト**: $256/月（Pinecone）  
**対象**: 全82キャラ

```bash
#!/bin/bash
# batch_ingestion.sh

# バッチ1: キャラ 1-20
echo "📦 バッチ 1/4: キャラ 1-20"
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 20 \
    --embedding-delay 0.5 \
    --llm-delay 1.0

echo "⏸️  5分待機..."
sleep 300

# バッチ2: キャラ 21-40
echo "📦 バッチ 2/4: キャラ 21-40"
python -m src.utils.ingest_excel_data \
    --start 21 \
    --end 40 \
    --embedding-delay 0.5 \
    --llm-delay 1.0

echo "⏸️  5分待機..."
sleep 300

# バッチ3: キャラ 41-60
echo "📦 バッチ 3/4: キャラ 41-60"
python -m src.utils.ingest_excel_data \
    --start 41 \
    --end 60 \
    --embedding-delay 0.5 \
    --llm-delay 1.0

echo "⏸️  5分待機..."
sleep 300

# バッチ4: キャラ 61-82
echo "📦 バッチ 4/4: キャラ 61-82"
python -m src.utils.ingest_excel_data \
    --start 61 \
    --end 82 \
    --embedding-delay 0.5 \
    --llm-delay 1.0

echo "✅ 全バッチ完了！"
```

実行:
```bash
chmod +x batch_ingestion.sh
./batch_ingestion.sh
```

---

## 🛠️ エラーが発生した場合

### ケース1: レート制限エラー

**エラーメッセージ**:
```
⚠️  レート制限検出。処理を一時停止します。
```

**対処**:
```bash
# 1. 5-10分待機
sleep 600

# 2. --resume オプションで再開
python -m src.utils.ingest_excel_data --resume

# または遅延を増やして再開
python -m src.utils.ingest_excel_data \
    --resume \
    --embedding-delay 1.5 \
    --llm-delay 3.0
```

### ケース2: 途中で中断した

**対処**:
```bash
# Ctrl+C で中断後、再開
python -m src.utils.ingest_excel_data --resume
```

### ケース3: 状態ファイルをリセットしたい

**対処**:
```bash
# バックアップ作成
cp data/excel_ingestion_state.json \
   data/excel_ingestion_state.backup.json

# ingested_entriesのみリセット（シート情報は保持）
# JSONエディタで編集するか、以下のPythonスクリプトで:
python -c "
import json
with open('data/excel_ingestion_state.json', 'r') as f:
    state = json.load(f)
state['ingested_entries'] = 0
state['failed_entries'] = 0
with open('data/excel_ingestion_state.json', 'w') as f:
    json.dump(state, f, indent=2, ensure_ascii=False)
print('✅ リセット完了')
"
```

---

## 📊 進捗確認方法

### 方法1: check_ingestion_progress.py

```bash
python check_ingestion_progress.py
```

**出力例**:
```
📊 Excel 取り込み進捗
─────────────────────
✅ 処理済みシート: 25/102
✅ 取り込みエントリ: 245
❌ 失敗エントリ: 3
⏱️  経過時間: 12分
📍 現在処理中: 50. ゲッコウガ - 行動フレーム
```

### 方法2: 状態ファイル直接確認

```bash
# JSON整形して表示
cat data/excel_ingestion_state.json | python -m json.tool
```

### 方法3: Pinecone確認

```bash
python test_pinecone_queries.py
```

---

## 💡 実行前の推奨設定

### オプション1: 保守的（安全重視）

```bash
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 20 \
    --embedding-delay 1.0 \
    --llm-delay 2.0 \
    --max-retries 5
```

**特徴**:
- ✅ レート制限に強い
- ✅ エラー発生率低い
- ⚠️  処理時間長い

### オプション2: バランス型（推奨）

```bash
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 40 \
    --embedding-delay 0.5 \
    --llm-delay 1.0 \
    --max-retries 3
```

**特徴**:
- ✅ 適度な速度
- ✅ 十分な安全性
- ✅ 推奨設定

### オプション3: 高速（リスク高）

```bash
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 82 \
    --embedding-delay 0.2 \
    --llm-delay 0.5 \
    --max-retries 2
```

**特徴**:
- ⚠️  レート制限リスク高
- ⚠️  エラー発生しやすい
- ⚠️  非推奨（テスト用のみ）

---

## 🎯 推奨実行フロー

### ステップ1: 動作確認（必須）

```bash
# ドライラン
python -m src.utils.ingest_excel_data --dry-run --start 1 --end 3
```

**確認項目**:
- [ ] Excelファイル読み込み成功
- [ ] シート抽出成功
- [ ] セクション分析成功
- [ ] エラーなし

### ステップ2: 小規模テスト（必須）

```bash
# 3キャラで実行
python -m src.utils.ingest_excel_data \
    --start 1 \
    --end 3 \
    --embedding-delay 1.0 \
    --llm-delay 2.0
```

**確認項目**:
- [ ] API呼び出し成功
- [ ] Pineconeアップロード成功
- [ ] 進捗ファイル更新成功
- [ ] レート制限エラーなし

### ステップ3: プラン選択（必須）

**質問**: どのプランで実行しますか？

- **プランA（テスト）**: 3キャラのみ → ステップ2で完了
- **プランB（選択）**: 20-30キャラ → 次へ
- **プランC（全体）**: 82キャラ全て → 次へ

### ステップ4: 本番実行

#### プランB（選択的）の場合

```bash
# 優先キャラリスト作成
nano priority_chars.txt
# 例: 1,3,6,7,8,11,14,18,21,25 と記載

# 実行
while read -r char_num; do
    echo "📦 キャラ $char_num 処理中..."
    python -m src.utils.ingest_excel_data \
        --start $char_num \
        --end $char_num \
        --embedding-delay 0.5 \
        --llm-delay 1.0
    
    echo "⏸️  30秒待機..."
    sleep 30
done < <(tr ',' '\n' < priority_chars.txt)
```

#### プランC（全体）の場合

```bash
# バッチスクリプト実行
./batch_ingestion.sh
```

### ステップ5: 結果確認（必須）

```bash
# 進捗確認
python check_ingestion_progress.py

# Pinecone確認
python test_pinecone_queries.py

# テストクエリ
python -c "
from src.brain.retriever import get_relevant_context
result = get_relevant_context('マリオの空前の発生フレームは？')
print(result)
"
```

---

## 📝 チェックリスト

### 実行前

- [ ] API キー設定確認
- [ ] 依存関係インストール確認
- [ ] Excelファイル存在確認
- [ ] Pinecone Index存在確認
- [ ] 十分なディスク容量（100MB以上）

### 実行中

- [ ] 進捗モニタリング
- [ ] エラーログ確認
- [ ] レート制限監視
- [ ] API コスト監視

### 実行後

- [ ] 全キャラ処理完了確認
- [ ] Pineconeベクトル数確認
- [ ] テストクエリで動作確認
- [ ] コスト集計
- [ ] ドキュメント更新

---

## 🆘 サポート

### よくある質問

**Q: どのプランを選べばいい？**
A: 初回は「プランA（テスト）」で動作確認後、コストに応じて「プランB（選択）」を推奨します。

**Q: 処理時間はどのくらい？**
A: プランAは2-3分、プランBは15-20分、プランCは45-90分です。

**Q: 月額コストは？**
A: プランAは$0、プランBは$50-80、プランCは$256です（主にPineconeストレージ）。

**Q: エラーが出たら？**
A: `--resume` オプションで再開できます。レート制限の場合は5-10分待ってから再開してください。

**Q: 途中で止めても大丈夫？**
A: はい、進捗は自動保存されるので `--resume` で再開できます。

### トラブル時の連絡先

詳細なドキュメント:
- [`EXCEL_INGESTION_RATE_LIMIT_SOLUTION.md`](./EXCEL_INGESTION_RATE_LIMIT_SOLUTION.md)
- [`INGESTION_IMPLEMENTATION_SPEC.md`](./INGESTION_IMPLEMENTATION_SPEC.md)

---

## 🎉 実行例（成功パターン）

```bash
$ python -m src.utils.ingest_excel_data --start 1 --end 3

📂 Loading Excel file: スマブラSP フレームデータ by検証窓.xlsx
✅ Found 102 character sheets
✅ Found 52 text files

======================================================================
📊 Excel Data → Pinecone Ingestion
======================================================================

[  1/101] マリオ
    ✅ 行動フレーム
    ✅ 能力値
    ✅ 滞空フレーム
  📊 進捗: 48/2460 (2.0%) | ❌ 0失敗 | ⚡ 0.8エントリ/秒 | ⏱️  残り49.0分

[  2/101] ドンキーコング
    ✅ 行動フレーム
    ✅ 能力値
    ✅ 滞空フレーム
  📊 進捗: 95/2460 (3.9%) | ❌ 0失敗 | ⚡ 0.8エントリ/秒 | ⏱️  残り49.2分

[  3/101] リンク
    ✅ 行動フレーム
    ✅ 能力値
    ✅ 滞空フレーム
  📊 進捗: 142/2460 (5.8%) | ❌ 0失敗 | ⚡ 0.8エントリ/秒 | ⏱️  残り48.3分

======================================================================
✅ Ingested: 142 | ❌ Failed: 0
📋 Sheets: 3
======================================================================

🎉 Done!
```

---

**このガイドを参考に、まずは「プランA（テスト実行）」から始めることを推奨します！**
